---
# Config file for testing

sources:

    # Used for testing CSV -> JSON, as well as unicode support.
    pokedex:
        v1:
            key: None
            gid: 0
            mapping:
                id: Pokedex ID
                name.en: English Name
                name.jp: Japanese Name
                type: Type
                characteristic.weight.kg: Weight (kg)
